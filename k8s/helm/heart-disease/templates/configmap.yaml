apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "heart-disease.fullname" . }}-config
  labels:
    {{- include "heart-disease.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
