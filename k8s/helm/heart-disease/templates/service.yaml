apiVersion: v1
kind: Service
metadata:
  name: {{ include "heart-disease.fullname" . }}
  labels:
    {{- include "heart-disease.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "heart-disease.selectorLabels" . | nindent 4 }}
